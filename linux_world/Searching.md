# Searching

Search for a pattern.

```bash
grep pattern files
```

Recursive search.

```bash
grep -r pattern dir
```

Filter command output.

```bash
command | grep pattern
```

Find file locations.

```bash
locate file
```
