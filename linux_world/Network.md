# Network

Ping a host.

```bash
ping host
```

Get domain registration information.

```bash
whois domain
```

Query DNS records.

```bash
dig domain
```

Reverse DNS lookup.

```bash
dig -x host
```

Download file.

```bash
wget file
```

Resume interrupted download.

```bash
wget -c file
```
